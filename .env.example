# Demo Mode (for testing and development)
# When enabled, all sync operations use mock data instead of real API calls
# Set to "true" to enable demo mode with fake financial data
TREELINE_DEMO_MODE=false
