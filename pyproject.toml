[project]
name = "treeline-money"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.11.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-asyncio>=1.2.0",
]
