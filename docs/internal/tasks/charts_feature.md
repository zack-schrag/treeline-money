Currently, the only way to display a chart is through the AI chat prompt, where it will use the chart pyplot MCP tools to determine displaying a chart based on the SQL query.

In addition to this, we want to allow a user to build a chart themselves from a sql query.

Acceptance criteria:
- Design proposal is created in docs/internal/proposals
- 