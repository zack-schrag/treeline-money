Currently, we have different keyboard shortcuts scattered through the terminal app. Instead, we should centralize into a single JSON file, so in the future it will be easy to add features for overriding. For example, a user may want to use vim keybindings.

Acceptnace criteria:
- All keybindings / shortcuts are consolidated into a single JSON configuration file. 
