The /tag mode has a bug where nothing happens when the suggested tags are selected.

Acceptance criteria:
- Users are able to tag transactions in /tag mode
- All other functionality in /tag mode still works, such as clearing tags, toggling tagged and untagged transactions, etc.